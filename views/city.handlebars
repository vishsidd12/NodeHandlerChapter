<div class="container">
        
          <div class="row">
              {{#each imagec}}
                <div class="col-md-6">
                    <div class="thumbnail">
                      <img src="/images/{{../city}}/{{../city}}-{{this}}.jpg"/>
                    </div>
                </div>
              {{/each}}
          </div>
      </div>